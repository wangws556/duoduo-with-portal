<UserControl x:Class="YoYoStudio.Client.Chat.Controls.MusicControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             xmlns:ctls="clr-namespace:YoYoStudio.Controls;assembly=YoYoStudio.Controls"
             Loaded="UserControl_Loaded">
    <Grid>
        <Border x:Name="PART_MusicBorder">
            <ctls:WpfFlexControl x:Name="flex"
                                 FlashCallback="flex_FlashCallback"
                                 MoviePath="{Binding MusicFlexPath}"/>
        </Border>
    </Grid>
</UserControl>
